#include"jarvis.hpp"

//./Jarvis
int main()
{
    Jarvis js;
    js.Init();
    js.Run();


    
    /*
    TuLing *tl = new TuLing();
    string message;
    while(1)
    {
        cout << "你要说什么 #";
        cin >> message;
        string result = tl->Chat(message);
        std:: cout << "Jarvis#" << result << std::endl;
    }*/
    return 0;
}
